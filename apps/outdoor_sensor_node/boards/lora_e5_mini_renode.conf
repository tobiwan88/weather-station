# Renode-specific overrides for outdoor_sensor_node.
# Disables hardware-dependent features that are not modeled in Renode,
# keeping only what's needed for boot verification and shell testing.

CONFIG_BME680=n
CONFIG_LORA=n
CONFIG_LORA_NODE=n
CONFIG_SENSOR_NODE_TX=n
CONFIG_PSA_CRYPTO=n
CONFIG_CRYPTO=n
CONFIG_CRYPTO_STM32=n
CONFIG_ENTROPY_GENERATOR=n
CONFIG_ENTROPY_STM32_RNG=n
CONFIG_I2C=n
CONFIG_GPIO=n
CONFIG_COUNTER=n
CONFIG_RTC=n
CONFIG_NVS=n
CONFIG_SETTINGS=n
CONFIG_SETTINGS_NVS=n
CONFIG_FLASH=n
CONFIG_FLASH_MAP=n
