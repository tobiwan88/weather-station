# Architecture Constraints

> Auto-generated by arch-sync from 16 ADRs on 2026-05-23.
> Do not edit manually. Run /arch-sync to regenerate.

## Active Constraints (14 ADRs accepted)

| ADR | Decision | Implementation Constraints |
|---|---|---|
| [ADR-001](../adr/ADR-001-repo-and-workspace-structure.md) | T2 topology — repo is both west manifest and Zephyr module. | • Zephyr version pinned in `west.yml`; upgrading requires testing all apps |
| [ADR-002](../adr/ADR-002-zbus-as-system-bus.md) | zbus is the sole IPC fabric. Channels owned by libraries, producers never know consumers. | • `ZBUS_CHAN_DEFINE` in exactly one `.c` per channel • Listeners run in publisher's context — must not block or sleep • Message structs must be copyable (no heap pointers) |
| [ADR-003](../adr/ADR-003-sensor-event-data-model.md) | Flat `env_sensor_data` struct with Q31 fixed-point. One event = one measurement. `sensor_uid` is the identity key. | • Only scalar measurements fit this model; vector/composite types need a different channel • No heap pointers — struct is copied by value through zbus |
| [ADR-004](../adr/ADR-004-trigger-driven-sampling.md) | Trigger-driven sampling. No sensor manager. Sensors subscribe to `sensor_trigger_chan`. | • No explicit constraints in Consequences — this is an enabling decision |
| [ADR-005](../adr/ADR-005-fake-sensor-subsystem.md) | Fake sensor subsystem as production-quality driver for `native_sim`. DT-defined, shell-interactive, self-registering. | • `CONFIG_FAKE_SENSORS` must never be `y` in production/release builds • Fake sensor UIDs must be 0x0001–0x00FF; real hardware starts at 0x0100 |
| [ADR-007](../adr/ADR-007-gateway-display-combined.md) | Gateway and display in one firmware image. zbus-only coupling between them. | • Display must use `DEVICE_DT_GET(DT_CHOSEN(zephyr_display))` — never a specific driver node • `lib/display_manager/` must have zero `#include` of anything from `lib/connectivity/` and vice versa |
| [ADR-008](../adr/ADR-008-kconfig-app-composition.md) | Apps compose features exclusively via Kconfig. No `target_link_libraries()` in app `CMakeLists.txt`. | • App `CMakeLists.txt` must never contain feature-selection logic • Library `CMakeLists.txt` may only gate on `if(CONFIG_...)` — no custom CMake variables • When in doubt, put it in a library — extracting later is cheap |
| [ADR-009](../adr/ADR-009-native-sim-first.md) | v1 targets `native_sim` exclusively. No real hardware required. | • Code using `k_busy_wait()` or precise microsecond timing must be guarded with `#if !defined(CONFIG_NATIVE_SIM)` • `CONFIG_FAKE_SENSORS=y` is coupled to `native_sim` by convention |
| [ADR-010](../adr/ADR-010-ci-and-dev-environment.md) | Single container image (`tobiwan88/zephyr_docker`) for both local dev and CI. | • Project is coupled to the `tobiwan88/zephyr_docker` image maintenance • CI runners must be GitHub-hosted Linux x86_64 |
| [ADR-011](../adr/ADR-011-http-dashboard.md) | HTTP dashboard on port 8080. Kconfig-opt-in, self-init via `SYS_INIT`. Spinlock + snapshot pattern for ring buffer. | • `k_spinlock` callbacks must not sleep or block — ring-buffer copy must complete in bounded time • HTML/CSS/JS must be self-contained (no CDN dependencies) |
| [ADR-012](../adr/ADR-012-integration-test-architecture.md) | Pytest integration tests via Twister harness. Page Object Model harnesses. One DUT per session. | • LVGL must be disabled in integration test builds • HTTP server needs ~1s settle time after boot before tests can hit endpoints • Only `--pytest-args` for pytest flags — no `pytest.ini` or `pyproject.toml` |
| [ADR-013](../adr/ADR-013-mqtt-configurable.md) | MQTT runtime config via `config_cmd_chan`. Base64-encoded passwords in settings under `config/mqtt/`. | • Base64 is not encryption — passwords are trivially recoverable • HTTP dashboard MQTT form accumulation is not thread-safe (tolerated on native_sim single-threaded HTTP) |
| [ADR-014](../adr/ADR-014-mcuboot-fota.md) | MCUboot FOTA with signed images, automatic rollback. UART + HTTP transports. Hardware-only feature. | • MCUboot reduces available flash per image slot — gateway feature set must stay within slot budget • `native_sim` target is unaffected — MCUboot and FOTA are hardware-only (`depends on BOOTLOADER_MCUBOOT`) |
| [ADR-015](../adr/ADR-015-lora-protocol.md) | LoRa protocol with AES-128-GCM + CRC-16. Button-press pairing. zbus-only integration. Supersedes ADR-006. | • Sequence number wrap requires periodic re-keying • UART proxy agent bridging gateway ↔ STM32WLE5JC is deferred |

### Constraint reference (by topic)

**zbus / IPC:**
- ADR-002: Sole IPC mechanism; channel ownership rules; listeners must not block; messages must be copyable
- ADR-004: Trigger-driven via `sensor_trigger_chan` and `sensor_event_chan`
- ADR-015: zbus-only integration for LoRa

**Data model:**
- ADR-003: Flat struct, Q31 fixed-point, one event = one measurement, no heap pointers
- ADR-013: Base64-encoded passwords (not encrypted), thread-unsafe HTTP form accumulation

**Sensor architecture:**
- ADR-004: No sensor manager; trigger-driven; each sensor independently subscribes
- ADR-005: Fake sensors for native_sim; DT-defined; UIDs 0x0001–0x00FF; never in production

**App composition:**
- ADR-001: T2 topology; Zephyr version pinned
- ADR-008: Kconfig-only composition; no CMake feature logic; library CMakeLists only CONFIG_ gates

**Display / Gateway:**
- ADR-007: Combined gateway+display in one image; zbus-only coupling; DT_CHOSEN for display

**Testing:**
- ADR-009: native_sim first; timing code guarded
- ADR-012: Pytest + Twister; LVGL disabled in tests; HTTP settle time; no pytest config files

**Connectivity:**
- ADR-013: MQTT via config_cmd_chan
- ADR-015: LoRa AES-128-GCM + CRC-16; button-press pairing; deferred UART proxy agent

**CI / Dev:**
- ADR-010: Single container image; GitHub-hosted runners

**Security / FOTA:**
- ADR-014: MCUboot FOTA; signed images; automatic rollback; hardware-only; slot budget

## Proposed Decisions (1 ADR proposed)

| ADR | Title | Status |
|---|---|---|
| [ADR-016](../adr/ADR-016-trace-recorder.md) | Thread Trace Recorder and Renode Performance Analysis | Proposed |

## Superseded Decisions

| ADR | Superseded by | Title |
|---|---|---|
| [ADR-006](../adr/ADR-006-lora-channel-boundary.md) | ADR-015 | LoRa Module Channel Boundary |
