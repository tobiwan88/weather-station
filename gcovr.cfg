# gcovr configuration for weather-station
# gcovr auto-discovers this file when --root points to its directory.
# Paths are matched relative to --root (/home/zephyr/workspace/weather-station).
# Filter restricts the report to lib/ sources only, excluding tests/ and Zephyr.
filter = lib/.*\.c$
