# SPDX-License-Identifier: Apache-2.0
#
# Aggregator CMakeLists.txt for lib/. Each sub-library is added conditionally by
# the module-level CMakeLists.txt via add_subdirectory_ifdef so nothing is
# needed here.
